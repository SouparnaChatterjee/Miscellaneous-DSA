class Solution {
public:
    int findPeakElement(vector<int>& a) 
    {
        // Code here.
        int n=a.size();
        int maxi=a[0];
        for(int i=0;i<n;i++)
        {
            if(a[i]>maxi)
            maxi=a[i];
        }
        return maxi;
    }
};
